#include <stdio.h>

int main() {
    int cp,sp;
    scanf("%d %d",&cp,&sp);
    if (sp-cp>0) printf("Profit");
    if else(sp-cp<0) printf("Loss");
    else printf("No Profit");
    return 0;
}