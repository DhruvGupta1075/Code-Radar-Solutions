#include <stdio.h>
#include <limits.h>

int getsec(int num[],int size){
    int max=INT_MIN;
    for (int i=0;i<size ;i++){
        if (arr[i]>max)
    }
}
int main(){
    int n;
    scanf("%d",&n);
    int arr[n];
    for (int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
}